# compound-ops

* rbind
    - new primary key rows
    - identical set of column names
* cbind
    - identical set of primary key
    - new columns
* overlay
    - subset of rows
    - subset of cols
    - block structure

# converters

* convert_to_data_table
