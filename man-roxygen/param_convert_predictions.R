#' @param convert_predictions (`logical(1)`)\cr
#'   If `TRUE` (default), the converts the internal [PredictionData] objects to
#'   regular [Prediction] objects.
#'   Uses all predict sets specified via `predict_sets`.
